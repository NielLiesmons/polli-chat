package com.polli.ui.theme

import androidx.compose.ui.text.font.FontFamily

/** Platform Inter family — Android loads bundled Inter; desktop falls back to sans-serif. */
expect val PolliFontFamily: FontFamily
